{% extends 'base.html.twig' %}

{% block title %}Armes{% endblock %}

{% block monTitre %}
Mon {{ arme.nom }}
{% endblock %}

{% block body %}
<div class="row">
    <div class="col-2">
        <img src="{{ asset('images/armes/' ~ arme.nom ~ '.png') }}" alt="">
    </div>
    <div class="col">
        <h2>{{arme.nom}}</h2>
        <h2>{{arme.description}}</h2>
        <h2>{{arme.degat}}</h2>
    </div>
</div>
{% endblock %}